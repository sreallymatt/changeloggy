
change "cli-bug" {
  body = "cli: `generate` - fix an issue with something"
}
change "internal" {
  body = "internal: `templatehelpers` - will it work this time?"
}
change "cli-bug" {
  body = "cli: `generate` - fix an issue with something else"
}
