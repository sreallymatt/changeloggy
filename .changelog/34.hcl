
change "cli" {
  body = "cli: `add` - entries can now be added in `hcl`, `yml`, or `md` formats"
}
change "cli" {
  body = "cli: `generate` - parses entries in `hcl`, `yml`, or `md` formats to generate the changelog"
}
change "cli" {
  body = "cli: `check` - checks for entries in `hcl`, `yml`, or `md` formats"
}
